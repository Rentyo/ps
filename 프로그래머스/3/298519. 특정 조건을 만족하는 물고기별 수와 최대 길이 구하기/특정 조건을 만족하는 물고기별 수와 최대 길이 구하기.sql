-- 코드를 작성해주세요
SELECT COUNT(*) as "FISH_COUNT", MAX(f.LENGTH) as "MAX_LENGTH", f.FISH_TYPE FROM (SELECT s.ID, s.FISH_TYPE, IFNULL(s.LENGTH, 10) as "LENGTH", s.TIME FROM FISH_INFO s) as f GROUP BY f.FISH_TYPE HAVING AVG(f.LENGTH) >= 33 ORDER BY FISH_TYPE